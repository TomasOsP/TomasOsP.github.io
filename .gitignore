# Jekyll generated output (GitHub Pages rebuilds this from source)
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby / Bundler
vendor/
.bundle/

# OS files
.DS_Store

# Temporary files
*.log
*.swp
*~

# Claude Code guidance (local only, not part of the published site)
CLAUDE.md

# Local pre-fix model backups (keep on disk, not in repo)
*.orig.glb
